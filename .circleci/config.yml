version: 2.1

workflows:
  version: 2
  circleci-build:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: docker:latest
    steps:
      - checkout
      - setup_remote_docker
      - run: apk add make
      - run: make
