version: 2.1

workflows:
  version: 2
  test:
    jobs:
      # - azlint
      - build

jobs:
  # azlint:
  #   docker:
  #     - image: matejkosiarcik/azlint
  #   steps:
  #     - checkout
  #     - run: azlint

  build:
    docker:
      - image: docker:latest
    steps:
      - checkout
      - setup_remote_docker
      - run: make build run
